"""Coqui VITS configuration and training entry points. Everything here
imports Coqui TTS lazily (inside functions), so the package imports fine
without `coqui-tts` — training itself runs on a GPU box."""
