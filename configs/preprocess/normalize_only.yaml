# readme.md section 1.3 "normalize-only" profile: cleaned and normalized
# text only. Identical to default.yaml today since normalized text is the
# only implemented layer beyond clean/sentences; kept separate so it stays
# stable as more profiles (pronunciation, expressive, tts-training, ...)
# are added in later phases.
include:
  - normalized
